# Ep02. 桌面环境

> cSpell:disable

## 配置系统

```bash
sudo pacman -Syu

# 安装 fastfetch lolcat cmatrix
# fastfetch -> 系统信息显示工具
# lolcat -> 彩虹文字显示工具
# cmatrix -> 矩阵雨屏幕特效
pacman -S fastfetch lolcat

# 安装 htop 交互式进程查看器
pacman -S htop

# 桌面环境
pacman -S gnome

# 安装组件
# gnome-desktop -> GNOME 桌面环境基础组件
# gdm -> GNOME 显示管理器
# ghostty -> 终端仿真器
# gnome-control-center -> GNOME 控制中心
# gnome-software -> GNOME 软件管理器
# flatpak -> 应用程序打包和分发系统
# xdg-desktop-portal-gnome -> GNOME 桌面门户服务
pacman -S gnome-desktop gdm ghostty gnome-control-center gnome-software flatpak xdg-desktop-portal-gnome

# 安装密码和密钥管理相关组件
# gnome-keyring -> GNOME 密钥环，用于存储密码和密钥
# libsecret -> 用于访问密码和密钥的库
sudo pacman -S gnome-keyring libsecret
# 启动并设置开机自启 GNOME Keyring 服务
systemctl --user enable --now gnome-keyring-daemon.service

# 启动并设置开机自启 GDM 服务
systemctl enable --now gdm
# 关闭图形界面登录
systemctl disable --now gdm
# 单次关闭
systemctl stop gdm

# 安装 Xorg 相关组件
sudo pacman -S xorg-server xorg-apps xorg-xinit
# 对于 GNOME on Xorg 会话，在 ~/.xinitrc 中添加
# export XDG_SESSION_TYPE=x11
# export GDK_BACKEND=x11
# exec gnome-session

# 删除不需要的软件包
pacman -Rns xorg-server xorg-apps xorg-xinit

# 安装字体
pacman -S ttf-sarasa-gothic # 思源黑体
pacman -S noto-fonts-emoji # 谷歌 Noto Emoji 字体

# Noto CJK 字体（支持中日韩字符）
pacman -S noto-fonts-cjk

# 一次性安装多个字体包
# sudo pacman -S ttf-sarasa-gothic noto-fonts-emoji noto-fonts-cjk

# MesloLGS Nerd Font
mkdir -p ~/.local/share/fonts
cd ~/.local/share/fonts
wget https://github.com/romkatv/powerlevel10k-media/raw/master/MesloLGS%20NF%20Regular.ttf
wget https://github.com/romkatv/powerlevel10k-media/raw/master/MesloLGS%20NF%20Bold.ttf
wget https://github.com/romkatv/powerlevel10k-media/raw/master/MesloLGS%20NF%20Italic.ttf
wget https://github.com/romkatv/powerlevel10k-media/raw/master/MesloLGS%20NF%20Bold%20Italic.ttf
fc-cache -fv
```

## 安装字体

```bash
# 安装常用字体包
sudo pacman -S noto-fonts noto-fonts-emoji adobe-source-han-sans-cn-fonts
```

# Yay - AUR 助手

Yay 是一个流行的 AUR 助手，用于简化从 Arch User Repository (AUR) 安装和管理软件包的过程

```bash
# 安装 Yay
# 安装依赖
pacman -S git base-devel
# 克隆 Yay 仓库
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
# 安装完成后，可以使用 yay 命令来安装 AUR 软件包
# 使用 Yay 安装 AUR 软件包
yay -S 包名

# 例如，安装 Google Chrome 浏览器
yay -S google-chrome
```

## 显卡驱动安装

```bash
# 安装头文件
pacman -S linux-headers

# 如果使用其他内核，比如 linux-zen 或 linux-lts，需要安装对应的头文件
# --needed 表示只有在未安装时才安装
pacman -S --needed linux-zen-headers linux-lts-headers
# ...

# intel 显卡驱动
# https://wiki.archlinuxcn.org/wiki/Intel_%E5%9B%BE%E5%BD%A2%E5%A4%84%E7%90%86%E5%99%A8
pacman -S mesa lib32-mesa vulkan-intel lib32-vulkan-intel
# 安装测试工具
sudo pacman -S mesa-utils
sudo pacman -S vulkan-intel vulkan-tools
# 测试 OpenGL 支持
glxinfo | grep "OpenGL renderer"
# 测试 Vulkan 支持
vulkaninfo | grep deviceName

# 硬件视频加速
# intel
pacman -S intel-media-driver
```

## 音视频服务和蓝牙

```bash
# 音频服务
# sof-firmware -> Sound Open Firmware，开源音频固件
# alsa-ucm-conf -> ALSA 使用者配置文件
# alsa-firmware -> ALSA 音频固件
sudo pacman -S sof-firmware alsa-ucm-conf alsa-firmware

# PipeWire 音频服务及其组件
# pipewire -> PipeWire 多媒体服务器
# wireplumber -> PipeWire 会话管理器
# pipewire-alsa -> PipeWire ALSA 插件
# pipewire-pulse -> PipeWire PulseAudio 插件
# pipewire-jack -> PipeWire JACK 插件
sudo pacman -S pipewire wireplumber pipewire-alsa pipewire-pulse pipewire-alsa pipewire-jack

# 启动并设置开机自启 PipeWire 及其 PulseAudio 插件服务
# --user 表示为当前用户启动服务，不用 sudo 提升权限
# pipewire -> PipeWire 多媒体服务器服务
# pipewire-pulse -> PipeWire 的 PulseAudio 插件服务
# wireplumber -> PipeWire 会话管理器服务
systemctl --user enable --now pipewire pipewire-pulse wireplumber

# 蓝牙服务
# bluez -> 蓝牙协议栈
# bluez-utils -> 蓝牙实用工具
sudo pacman -S bluez bluez-utils
# 启动并设置开机自启蓝牙服务
sudo systemctl enable --now bluetooth

# 电源管理服务
# power-profiles-daemon -> 电源配置守护进程
sudo pacman -S power-profiles-daemon
sudo systemctl enable --now power-profiles-daemon
```

## 安装中文输入法

```bash
# https://github.com/SHORiN-KiWATA/ShorinArchExperience-ArchlinuxGuide/wiki/%E4%B8%AD%E6%96%87%E8%BE%93%E5%85%A5%E6%B3%95

# fcitx5-im -> 包含了fcitx5的基本包
# fcitx5-chinese-addons -> 包含了多种中文输入法引擎和相关插件
# fcitx5-mozc -> Google日文输入法的fcitx5
pacman -S fcitx5-im fcitx5-chinese-addons fcitx5-mozc

# 配置环境变量
# 编辑 /etc/environment 文件
# 添加以下内容:
vim /etc/environment
GTK_IM_MODULE=fcitx     # 设置 GTK 应用程序使用 fcitx 输入法框架
QT_IM_MODULE=fcitx      # 设置 Qt 应用程序使用 fcitx 输入法框架
XMODIFIERS=@im=fcitx    # 设置 X 应用程序使用 fcitx 输入法框架
```

## 通过 pacman 安装桌面软件

```bash
# 安装 Firefox 浏览器
sudo pacman -S firefox
```

## 文件系统快照

```bash
# 安装 snapper 及相关工具
# snapper -> 文件系统快照和回滚工具
# snap-pac -> 用于与 pacman 集成的 snapper 插件
# btrfs-assistant -> Btrfs 文件系统管理工具
# grub-btrfs -> 将 Btrfs 快照集成到 GRUB 启动菜单
# inotify-tools -> 文件系统事件监控工具
pacman -S snapper snap-pac btrfs-assistant grub-btrfs inotify-tools
# 启动并设置开机自启 grub-btrfsd 服务
systemctl enable --now grub-btrfsd

# 重启
reboot

# 创建名为 root 的快照配置，针对根目录 /
# -c -> 指定配置名称
# create-config -> 创建配置
snapper -c root create-config /
snapper -c home create-config /home
# 查看快照配置
snapper list-configs

# 创建快照
snapper -c root create -d "Hello Root"
snapper -c home create -d "Hello Home"
# 列出快照
snapper -c root list
snapper -c home list

# 安装 LTS 内核以提高系统稳定性
pacman -S linux-lts

# 将快照集成到 GRUB 启动菜单
grub-mkconfig -o /boot/grub/grub.cfg
```
